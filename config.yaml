models:
  - name: DeepSeek R1 14B (Remote PC)
    provider: ollama
    model: deepseek-r1:14b
    apiBase: http://YOUR_PC_IP:11434
      # Replace with the LAN IP of the machine running ollama.
      # Never commit a real private IP here — this repo is public.
